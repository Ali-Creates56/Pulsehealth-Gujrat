/**
 * PulseHealth Gujrat — Configuration Template
 * 
 * SETUP INSTRUCTIONS:
 * 1. Copy this file and rename it to `config.js`
 * 2. Replace 'your_mapbox_token_here' with your actual Mapbox access token
 * 3. Get a free token at: https://account.mapbox.com/access-tokens/
 * 
 * ⚠️ NEVER commit config.js to version control — it is gitignored.
 */
window.__CONFIG__ = {
  MAPBOX_TOKEN: 'your_mapbox_token_here'
};
